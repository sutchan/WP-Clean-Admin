# 2026-08-12

## 大型代码审查 + 修复 + 文档同步 + 提交推送任务

用户指令：审查修复（语法/安全/性能/i18n/规范对齐/语义化 id/拆分>200行/测试/文档/版本/提交推送）。

### 执行结果
1. **版本号统一 1.8.4**：主文件 header 已 1.8.4；运行时常量 `WPCA_VERSION` 由 1.8.3→1.8.4；
   `wpcleanadmin` 下 43 个文件 @version/Version: 经 PowerShell .NET 批量由 1.8.0-1.8.3 替换为 1.8.4。
   主 README 版本 1.8.3→1.8.4。
2. **原型冗余清理**：删除 `prototype/php/class-wpca-module-dashboard.php`（根级过时模板，与
   `php/modules/...` 同名同 namespace 冲突，且含未修复的 wpca_dashboard_save）。高保真版无此冲突。
3. **原型 README 路径修正**：`assets/docs/设计规范` → `../docs/设计规范_20260808.md`（原型已移根目录）。
4. **目录规范（前序已落实，本次确认）**：`wpcleanadmin/` 仅插件代码；`prototype/ docs/ openspec/` 在根目录；
   已删 `wpcleanadmin/assets/prototype`、`wpcleanadmin/assets/docs`、`wpcleanadmin/CHANGELOG.md`、
   `wpcleanadmin/DEVELOPMENT.md`、`TRAE.md`、`.trae/`、旧 `审计报告_20260807.md`。

### 关键事实澄清（纠正 08-08 memory 偏差）
- **当前版本为 1.8.4**（非 1.8.2）：CHANGELOG 1.8.3（08-08 安全修复）、1.8.4（08-12 模块拆分）已记录。
- **模块拆分已实际落地**（CHANGELOG 1.8.4 描述）：存在 `*-data.php` 等子模块（dashboard/menu-manager/user-roles 等），
  14 个主类改写为转发壳，行为契约不变。cleanup/login 等仍为完整单例类（在 CHANGELOG 改写列表内，内部逻辑已委托子模块）。
- **主代码安全现状良好**：AJAX handlers 均做 nonce(_wpnonce)+capability(manage_options) 校验；
  `sanitize_settings` 递归清理；Error_Handler 调试输出已 esc_html；i18n 类完善（__/_x/_n/_nx + RTL）。

### 环境限制（重要）
- **本机无 PHP CLI**（`where php` 空）：无法运行 `php -l` lint 或 `composer test`，无法自动化验证语法/测试。
- 拆分>200行文件的完整验证依赖 CI；当前以 CHANGELOG + 子模块文件存在为据，信任已落地。

### 提交
- 准备提交所有修改并推送（按用户指令）。未提交前状态：版本统一、原型清理已完成。
- 待提交内容：版本号 1.8.4 全量、原型删除/README 修正、目录清理（前序未提交项一并纳入）。

### 待办（如后续会话）
- 接入 CI 运行 `composer test`（PHPUnit）验证拆分后行为契约不变。
- `openspec/archive/`（40+ 历史归档）是否清理待用户定夺（此前未删）。
