# 2026-08-08

## 创建插件原型与设计规范（高保真）

用户要求"继续创建高保真项目原型，和项目设计规范"。在 2026-08-07 基础骨架之上升级。

### 交付物
1. 设计规范（完整版）：`wpcleanadmin/assets/docs/设计规范_20260808.md`
   - 信息架构（6 页签：仪表盘/清理/性能/安全与菜单/数据库/诊断/设置）
   - 设计令牌（:root 变量）、排版、响应式断点 782/600px
   - 组件库 BEM `.wpca-*`（nav/card/stat/table/badge/btn/switch/progress/empty/list/modal/toast）
   - 交互规范（AJAX 数据流 _wpnonce + capability；危险操作二次确认 modal；空/错误态）
   - 可访问性 WCAG AA（键盘聚焦、label 关联、aria-live、弹窗 role=dialog+焦点陷阱）
   - 编码与目录规范、原型-实现映射表

2. 高保真 PHP 原型：`assets/prototype/php/`
   - class-wpca-module-base.php（抽象基类，单例+register_ajax+sanitize_settings）
   - class-wpca-ajax-gateway-base.php（统一 _wpnonce 网关，dispatch 校验 nonce+capability）
   - modules/ 下 6 个真实感模块：Dashboard(体检指标)、Cleanup(6任务)、Performance(5开关)、Security(菜单可见性树)、Database(5表+备份/优化)、Diagnostics(6环境检测)
   - 注意：database 模块初版有引号语法错误（`wp_users'`），已修正为 `'wp_users'`

3. 高保真前端 UI：`assets/prototype/ui/`
   - dashboard.html（多页签布局，含 modal+toast 容器）
   - wpca-components.css（完整组件库，响应式）
   - app.js（页签切换、mock 数据渲染、统一 api() 封装 _wpnonce、进度条、toast、modal 二次确认；USE_MOCK=true 可无后端预览）

4. 测试升级：`tests/PrototypeTest.php`（覆盖抽象类/网关单例/6模块 get_module_name/Dashboard 指标结构）

5. 索引：`assets/prototype/README.md`

### OpenSpec
- `openspec/changes/plugin-prototype/` 下 proposal/tasks/design 三件套已建（tasks 已更新为高保真范围）
- 设计规范另存于 `assets/docs/`（非 openspec 内，因属资产）

### 环境限制
- 本机无 php/phpunit CLI，未实跑 `composer test`；PHP 文件语法无法本地 lint（已人工修正已知错误）。
- 原型独立存放 assets/prototype/，未改主代码 1.8.2。

## 原型与 openspec 规范同步（2026-08-08 续）

用户要求更新 /prototype 与 /openspec 使两者严格对应、消除歧义。

### 原型更新（assets/prototype/）
- dashboard.html：新增「设置」第 7 页签 + 设置表单（general/performance 组）+ nonce 隐藏字段（name=_wpnonce，浏览器预览用占位回退）
- app.js：新增设置保存逻辑（wpca_settings_save）、getNonce 改为按 name 取 _wpnonce
- wpca-components.css：补 .wpca-fieldset/.wpca-field 样式
- 新增 modules/class-wpca-module-settings.php（Settings.config 数据结构，合并默认值 R4）
- 设计规范_20260808.md：新增 §8 AJAX 接口标准、§9 数据结构 Schema、§10 业务规则 R1-R6；IA 扩为 7 页
- PrototypeTest.php：测试覆盖 7 模块
- README.md：补设置模块与 AJAX 契约

### openspec 修订
- api/spec.md：版本 1.8.1→1.8.2；§2.4 新增 AJAX 接口标准（_wpnonce/wpca_ajax_nonce、错误码映射、已注册 action 一览、业务数据结构、Settings.config）；§2.2 命名空间改为模块化；版本历史补 1.8.2
- plugin-architecture/design.md：新增决策 5（统一 AJAX 网关）、决策 6（原型基线）；架构层次改为模块化 Modules\* + Gateway_Base
- plugin-architecture/detailed-design.md：版本 1.8.0→1.8.2（项目概述/主文件/常量/autoload/core 类）；§7.7 新增 AJAX 网关标准 + 模块→action 映射
- changes/plugin-prototype/tasks.md：新增阶段五（规范同步）

### 关键一致性结论
- nonce 字段名全链路统一 `_wpnonce` + action `wpca_ajax_nonce`（原型/规范/前端一致）
- 7 模块 ↔ 10 个 AJAX action 映射在原型、api.md、detailed-design.md 三处一致
- 版本号全文档统一 1.8.2（主代码已 1.8.2）

## 代码审查（2026-08-08 第三次）

用户要求"审查项目"。输出审计报告：`wpcleanadmin/assets/docs/审计报告_20260808.md`。

### 关键发现（P0-P3）
- **P0-1 严重**：`includes/class-wpca-ajax.php` 声明空实现 IDE stub（`wp_verify_nonce(){}`），
  dispatcher 级未真正调用 `check_ajax_referer`/`current_user_can`，非 WP 环境下 nonce 校验形同虚设。
- **P0-2 严重**：action 冲突 `wpca_dashboard_save` 同时被 dashboard 模块（存原始 clean）与
  settings-page.js（实为全局设置）使用，与 Settings 模块 `wpca_settings_save`（存 merged 结构）
  职责重叠、数据结构不一致。建议删除 dashboard 的该 action，统一走 Settings。
- **P1-1**：原型 `Module_Base::register_ajax` 直接 `add_action`，未串联 `Gateway_Base::dispatch` 守卫链，
  与规范"统一网关"声明矛盾。
- **P1-2**：前端 app.js 多处 `innerHTML` 拼接动态文本（metric/count/label/advice），存在 XSS 风险，须转义。
- **P1-3**：测试覆盖率远低于 80%，本机无 PHP CLI 未实跑 `composer test`。
- **P2**：旧式类与模块并存；设计规范与 api.md Dashboard 职责错位；dashboard.html/settings-page.html 双入口重复。
- **P3**：database 模块白名单校验为 TODO；app.js `MOCK_NONCE` 缺环境检测。

### 结论
主代码（1.8.2）历史修复到位，但原型+模块化架构引入 P0 级安全/一致性回退，优先修 P0-1/P0-2/P1-1。
代码均未提交（按规则不主动 commit）。

## 修复执行（2026-08-08 续）

用户要求"继续修复"。已修复审计报告的实质问题：

### 实际修复项
- **P0-2 已修**：删除 `class-wpca-module-dashboard.php` 的 `wpca_dashboard_save` 注册+handler；
  `settings-page.js`/`settings-page.html` 统一改用 `wpca_settings_save`，发送 Settings 模块期望的
  `general/performance/menu` JSON 结构（与 `class-wpca-module-settings.php` 对齐）。
- **P1-2 已修**：`app.js` 全部动态渲染（renderStats/renderCleanup/renderPerf/renderMenus/renderDb/诊断）
  由 `innerHTML` 拼接改为 `el()` 辅助 + `textContent` DOM 构建，消除 XSS 风险。新增 `el(tag,cls,text)` 工具。
- **P2-2 已修**：`openspec/specs/api/spec.md` 移除 `wpca_dashboard_save` 行（已并入 Settings）。

### 复核结论（误判降级）
- **P0-1 复核为误判**：`class-wpca-ajax.php` 空 stub `wp_verify_nonce(){}` 在 WP 未加载时返回 null →
  nonce 校验失败→拒绝请求（安全方向正确，非绕过）。各 handler 已在方法内逐个做 nonce+capability 校验。
  报告措辞已修正，stub 保留。
- **P1-1 复核为误判**：`Module_Base::register_ajax` 实际已调用 `Gateway_Base::register`，网关串联成立。

### 遗留
- P1-3 测试缺口：本机无 PHP CLI 未实跑 `composer test`，覆盖率远低于 80%。
- P2 旧式类/模块化并存、dashboard.html/settings-page.html 双 UI 入口待清理。

代码均未提交（按规则不主动 commit，待用户确认）。

## 第三次审查：数据丢失与恢复（2026-08-08）

用户再次"审查项目"。发现严重事件：

### 严重事件：prototype 数据丢失（已恢复）
- 会话中段重构任务中断，导致 `assets/prototype/` 下 16 个文件从工作区被删（git status 显示 16×`D`），
  仅剩新建未跟踪的 `ui/index.html`。原型当时完全无法运行（缺 wpca-components.css/app.js）。
- 原因：前轮"继续修复"的写入（settings-page 删除、app.js XSS 修复）从未持久化，且后续删除未回滚。
- **处置**：`git -C e:\Github\WPCleanAdmin checkout HEAD -- wpcleanadmin/assets/prototype`
  从 HEAD 还原全部 16 文件，原型恢复可运行。index.html 保留。
- 还原的是 HEAD 旧版：app.js 仍是 P1-2 XSS 旧版（innerHTML），dashboard.html 仍是旧入口，
  P0-2（wpca_dashboard_save 冲突）在 HEAD 仍未修。api.md 之前已移除该 action，故文档/代码错位。

### 当前待执行（用户上轮"精简+高保真+设计规范"要求的未完成部分）
1. 删 `dashboard.html` + `settings-page.*`，统一 `index.html` 入口（P0 结构冗余）。
2. app.js 重做 XSS 安全渲染（el()+textContent）。
3. 修 P0-2：dashboard 模块移除 wpca_dashboard_save，统一 wpca_settings_save（与 api.md 对齐）。
4. 创建 `DESIGN.md` 设计规范（设计系统/组件库/交互标准）—— 尚未创建。
5. 重写 `wpca-components.css` 极简顶尖风格 + 移动端响应式。

### 关键教训
- 重写/删除类任务必须写完即落盘再切换，避免中断丢失（上一轮 app.js XSS 修复因未持久化而丢失）。
- 每次批量文件操作后建议立即 `git status` 验证落盘。
- 报告：wpcleanadmin/assets/docs/审计报告_20260808.md 已更新为第三次审查版。

## 目录边界规范与清理（2026-08-08 续）

用户明确规范：**`/wpcleanadmin` 为插件目录，只放插件代码文件；其它项目文件（原型/文档/规范/项目 README）放根目录。**

### 执行（均从插件目录移出，未提交）
- 删除 `wpcleanadmin/assets/prototype/`（17 文件）→ 根 `/prototype/` 为正确副本（含 index.html 高保真版）。
- 删除 `wpcleanadmin/assets/docs/审计报告_20260808.md` → 根 `/docs/` 为正确副本。
- 删除 `wpcleanadmin/CHANGELOG.md`、`wpcleanadmin/DEVELOPMENT.md` → 根目录唯一源。
- 保留 `assets/css/`、`assets/js/`（插件后台样式/脚本，属插件代码）。
- 保留 `languages/README.md`、`includes/modules/README.md`（贴近代码的模块说明）。
- `AGENTS.md` §2 新增「目录边界约定」固化此规则。

### 最终插件目录 = 纯插件代码（wp-clean-admin.php / includes/ / assets/{css,js} / languages/ / tests/ / composer.json / phpunit.xml.dist）
### 根目录项目文件 = /prototype /docs /openspec /CHANGELOG.md /DEVELOPMENT.md /README.* /AGENTS.md /LICENSE

## 应用目标目录结构（2026-08-08 续）

用户要求"应用"目标结构：`wpcleanadmin/{wp-clean-admin.php, includes/, assets/{css,js}/, languages/, tests/, composer.json, phpunit.xml.dist}`。

### 核查结果
- 全量搜索 wpcleanadmin/（104 文件）均属清单范围：主文件、includes(含 ajax/modules/settings)、
  assets/css、assets/js、languages(.po/.mo/.pot)、tests、composer.json、phpunit.xml.dist。
- **保留项（非代码但必需/贴近代码）**：
  - `wpcleanadmin/.gitignore` —— git 仓库配置，必须保留。
  - `wpcleanadmin/languages/README.md`、`wpcleanadmin/includes/modules/README.md` —— 模块说明文档，
    贴近代码、非独立项目文档，保留（不违反"剥离原型/审计报告/CHANGELOG"意图）。
- **结论**：插件目录已严格符合用户目标结构，无需删除任何插件代码文件。

### 待办（上轮要求仍待执行，未提交）
- app.js XSS 安全渲染、P0-2 移除 wpca_dashboard_save、删根 prototype 内 dashboard.html+settings-page.*、
  创建 /prototype/DESIGN.md、重写 wpca-components.css 极简响应式。均须写完即落盘。

## 第四次审查（2026-08-08 续）

用户再次"审查项目"。核查根 prototype/，发现结构冗余 + 引用失效（原型移出插件目录后）：

### 冗余（待删，需批准）
- `php/class-wpca-module-dashboard.php`（根级旧模板）与 `php/modules/...dashboard.php` 同名类冲突；
  前者仍注册 wpca_dashboard_save（P0-2 未修）+ include 已删的 settings-page.html + 引用无效 assets 路径。
- `ui/dashboard.html`（旧入口）与 `ui/index.html`（新）并存 → 删 dashboard.html。
- `ui/settings-page.css` 孤立（settings-page.html 已删）→ 删。

### 引用失效（路径修正待执行）
- index.html:4 注释指向 `assets/prototype/DESIGN.md`（DESIGN.md 未创建，应为 docs/设计规范_20260808.md）。
- dashboard 模块 include `WPCA_PLUGIN_DIR . 'assets/prototype/ui/...'` 路径失效（原型已不在插件 assets）。
- README.md 多处引用 `assets/docs/设计规范_20260808.md`、`ui/dashboard.html` 应改为 `docs/...`、`ui/index.html`。

### 现状
- `docs/设计规范_20260808.md`（7.85KB）已存在；`/prototype/DESIGN.md` 未创建。
- wpcleanadmin/ 已是纯插件代码（符合规范）；无争议冗余（旧审计报告、.trae）已删。
- 报告：docs/审计报告_20260808.md 第九章。删除/移动动作仍待用户明确批准。

## 清理重复冗余文档（2026-08-08 续）

用户要求"清理重复冗余文档"。扫描全项目 69 个 .md。识别冗余：

### 明确冗余（建议删，无争议）
- `docs/审计报告_20260807.md`：被 `审计报告_20260808.md` 完全覆盖。
- `.trae/`（rules + 4 skills）：已被 `.codebuddy/` 取代。

### 需确认（未擅删）
- `openspec/archive/`（40+ 归档文档）：与现行 openspec/specs 重叠，疑为有意保留基线。
- `openspec/AGENTS.md` vs 根 `AGENTS.md`：职责不同（根=总览，openspec/=操作），建议保留。

### 保留（贴近代码，非冗余）
- `wpcleanadmin/languages/README.md`、`wpcleanadmin/includes/modules/README.md`、`prototype/README.md`。

**关键事件**：提交删除 `.trae/` + 旧审计报告命令时**用户拒绝执行**。→ 此后不再主动删除，
改为只报告冗余项并请用户确认。删除类操作须先获明确批准。

### 待办（上轮要求仍未完成，因中断丢失）
- app.js XSS 安全渲染（el+textContent）
- P0-2 dashboard 模块移除 wpca_dashboard_save
- 删根 prototype 内旧 dashboard.html + settings-page.*，统一 index.html
- 创建 /prototype/DESIGN.md 设计规范（设计系统/组件库/交互标准）
- 重写 wpca-components.css 极简顶尖风格 + 移动端响应式
（均须写完即落盘，避免再次丢失）

## 设计系统升级 + 原型合并 + 文档对齐（2026-08-08 续）

用户要求：完善高保真原型、建立设计规范、shadcn/ui 风格组件库、极简顶尖 UI、去冗余、
代码与原型对齐、文档同步、全面测试。已确认技术路线：**PHP 版 shadcn 风格设计系统**
（非 React，因本插件是 WordPress PHP）；原型冗余 **合并为单套并删除**。

### 执行成果
1. **shadcn 设计系统**（单一令牌来源）：重写 `prototype/ui/wpca-components.css`
   - HSL 通道令牌（--wpca-primary 等，支持 prefers-color-scheme 深色）
   - 组件库：card/btn/badge/switch/input/fieldset/field/table/list/stats/progress/empty/modal/toast/col-*
   - 导航顶栏玻璃拟态、侧栏（<960px 转抽屉）、响应式 960/600、reduced-motion
2. **原型合并去冗余**：删除 `ui/dashboard.html`、`ui/settings-page.css`；`index.html` 为唯一入口
   （补 `#wpca-nonce` 占位、移动端 `#wpca-nav-toggle` 抽屉事件）；`app.js` 补 nonce 兼容 + 加载态 + 诊断宽列。
3. **插件代码对齐**：`wpcleanadmin/assets/css/wpca-admin.css` 末尾追加同名 shadcn 层（与原型同源，
   独立于旧 .wpca-tabs/.wpca-button 体系，无冲突）。
4. **文档同步**：`docs/设计规范_20260808.md` 升级令牌为 HSL、组件库 shadcn 化、修正原型路径；
   `prototype/README.md` 改单套结构说明；OpenSpec `design.md/api.md/detailed-design.md/tasks.md/proposal.md`
   的 `assets/prototype/` 陈旧路径统一改为 `prototype/`（对齐 AGENTS.md 目录边界）。
5. **Bug 修复（全面测试）**：`prototype/php/modules/class-wpca-module-dashboard.php` 的
   `render_page()` 从 include 已删的 `assets/prototype/ui/dashboard.html` 改为 `prototype/ui/index.html`（加存在性校验）；
   `wpcleanadmin/tests/PrototypeTest.php` require 路径改为真实 `prototype/php/`。
6. **静态交叉验证**：app.js 全部 `#wpca-*`/`.wpca-*` 选择器与 index.html 元素一一对应；
   注入类名在 CSS 均有定义；设置页字段名与 app.js 读取一致。

### 环境限制
- 本机无 php/python CLI，playwright 默认阻止 file:，未能浏览器实跑；以静态一致性校验替代。
- 代码未提交（按规则不主动 commit）。

## 全量代码审查 + 修复 + 版本同步（2026-08-08 终轮）

用户要求：审查语法/安全/性能、i18n、对齐 openspec/prototype、拆分>200行、测试、文档、版本、冲突、提交main。

### 关键架构事实（重要，决定修复策略）
- **modular 架构仅实际加载 Settings 模块**：`class-wpca-core.php::load_modular_modules()` 只 `new WPCleanAdmin\Modules\Admin\Settings\Settings`，其余功能全靠 `load_legacy_modules()` 加载根目录 legacy 类（`WPCleanAdmin\Core/Dashboard/...`）。→ 根目录 legacy 类是功能核心，**不可删除**（openspec 称"legacy 下版删除"但当前仍是实际载体）。
- **AJAX nonce 契约真实 bug**：前端 `class-wpca-dashboard.php`（legacy+modular 两处）`wp_create_nonce('wpca_dashboard_nonce')`，但所有 AJAX handler 校验 `wpca_ajax_nonce` → Dashboard 全部 AJAX 失败。

### 本轮已提交修复（03bca42，1.8.2→1.8.3）
1. **XSS**：`modules/core/classes/class-wpca-error-handler.php::exception_handler` 调试输出改用 `esc_html()`。
2. **nonce 对齐**：两处 dashboard `wp_create_nonce('wpca_dashboard_nonce')` → `'wpca_ajax_nonce'`（与后端契约一致）。
3. **composer author**：`composer.json` 作者名 `Sut` → `Tanox`。
4. **版本号**：全项目 1.8.2→1.8.3（89 文件；排除 CHANGELOG 单独加小节、openspec/archive 归档、.codebuddy/memory 误改已还原）。
5. CHANGELOG 新增 1.8.3 小节；git push origin/main 成功。

### 待用户在 WP 环境验证/后续轮处理（高风险，未盲改）
- **i18n 全量**：legacy 类存在硬编码中文字符串未用 `__()` 包裹，需逐文件标记翻译。
- **拆分>200行文件**：24 个 PHP（login 785/menu-manager 748/settings 552 等）+ JS/CSS；因 modular 命名空间已占满同类名，拆分需谨慎设计新类/ trait，避免冲突。
- **运行测试**：本机无 php，无法实跑 `composer test` 或 WP 集成测试。
- **架构统一**：legacy→modular 全量迁移需先让 modular 补齐缺失模块（当前 modular 仅 Settings），属重大重构，需运行环境验证。

### 分支
- main 已领先 origin/main 多个提交；本轮回合前无冲突（工作区 clean），推送后 0 ahead/0 behind。
- 环境无 php，用 `git status`/`git diff` 静态核对，未做 lint。
